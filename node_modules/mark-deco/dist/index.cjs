/*!
 * name: mark-deco
 * version: 1.3.0
 * description: Flexible Markdown to HTML conversion library
 * author: Kouji Matsui (@kekyo@mi.kekyo.net)
 * license: MIT
 * repository.url: https://github.com/kekyo/mark-deco
 * git.commit.hash: 6c448464efddc8483fa3496393a67728e9fa4765
 */

"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const __screwUpDefaultImportModule0$2 = require("js-beautify");
const __screwUpDefaultImportModule1 = require("rehype-stringify");
const __screwUpDefaultImportModule2 = require("remark-gfm");
const __screwUpDefaultImportModule0$1 = require("remark-parse");
const __screwUpDefaultImportModule4 = require("remark-rehype");
const unified = require("unified");
const unistUtilVisit = require("unist-util-visit");
const jsYaml = require("js-yaml");
const htmlGenerator = require("./html-generator-CHbU5jbh.cjs");
const __screwUpDefaultImportModule0 = require("rehype-pretty-code");
const shiki = require("shiki");
const utils = require("./utils-DBRV1kXj.cjs");
const htmlGenerator$1 = require("./html-generator-DTRmTXLk.cjs");
const asyncPrimitives = require("async-primitives");
function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: "Module" } });
  if (e) {
    for (const k in e) {
      if (k !== "default") {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}
const __screwUpDefaultImportModule1__namespace = /* @__PURE__ */ _interopNamespaceDefault(__screwUpDefaultImportModule1);
const __screwUpDefaultImportModule2__namespace = /* @__PURE__ */ _interopNamespaceDefault(__screwUpDefaultImportModule2);
const __screwUpDefaultImportModule0__namespace$1 = /* @__PURE__ */ _interopNamespaceDefault(__screwUpDefaultImportModule0$1);
const __screwUpDefaultImportModule4__namespace = /* @__PURE__ */ _interopNamespaceDefault(__screwUpDefaultImportModule4);
const __screwUpDefaultImportModule0__namespace = /* @__PURE__ */ _interopNamespaceDefault(__screwUpDefaultImportModule0);
function parseFrontmatter(content) {
  const frontmatterRegex = /^---\r?\n([\s\S]*?)\r?\n---(?:\r?\n|$)/;
  const match = content.match(frontmatterRegex);
  if (!match) {
    return {
      data: {},
      content
    };
  }
  const yamlContent = match[1] || "";
  const markdownContent = content.slice(match[0].length);
  try {
    const parsedData = jsYaml.load(yamlContent, { schema: jsYaml.JSON_SCHEMA });
    const data = parsedData && typeof parsedData === "object" && !Array.isArray(parsedData) ? parsedData : {};
    return {
      data,
      content: markdownContent
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : "Unknown YAML parsing error";
    throw new Error(`Failed to parse frontmatter YAML: ${errorMessage}`);
  }
}
function stringifyFrontmatter(frontmatter) {
  if (!frontmatter || Object.keys(frontmatter).length === 0) {
    return "";
  }
  const yaml = jsYaml.dump(frontmatter, { lineWidth: 0 });
  if (typeof yaml !== "string" || yaml.length === 0) {
    return "";
  }
  return yaml.endsWith("\n") ? yaml : `${yaml}
`;
}
function composeMarkdownFromParts(frontmatter, content) {
  const body = content != null ? content : "";
  const yaml = stringifyFrontmatter(frontmatter);
  const frontmatterSection = `${"---"}
${yaml}${"---"}
`;
  return `${frontmatterSection}${body}`;
}
const NO_OP_LOGGER_FUNCTION = () => {
};
const NO_OP_LOGGER = {
  debug: NO_OP_LOGGER_FUNCTION,
  info: NO_OP_LOGGER_FUNCTION,
  warn: NO_OP_LOGGER_FUNCTION,
  error: NO_OP_LOGGER_FUNCTION
};
const CONSOLE_LOGGER = {
  debug: console.debug,
  info: console.info,
  warn: console.warn,
  error: console.error
};
const getNoOpLogger = () => NO_OP_LOGGER;
const getConsoleLogger = () => CONSOLE_LOGGER;
const rehypePrettyCode = __resolveDefaultExport$2(__screwUpDefaultImportModule0__namespace);
globalThis.__screwUpIsInCJS_cbf887d3405a = true;
function __resolveDefaultExport$2(module2, isESM) {
  const __isInCJS = typeof globalThis !== "undefined" && globalThis.__screwUpIsInCJS_cbf887d3405a === true;
  const maybe = module2;
  const hasDefault = !!(maybe && typeof maybe === "object" && "default" in maybe);
  const unwrapNamespaceDefault = (value) => {
    if (!value || typeof value !== "object") {
      return value;
    }
    const inner = value;
    const isModule = inner.__esModule === true || typeof Symbol !== "undefined" && inner[Symbol.toStringTag] === "Module";
    if (isModule && "default" in inner) {
      return inner.default;
    }
    return value;
  };
  const resolvedDefault = hasDefault ? unwrapNamespaceDefault(maybe.default) : void 0;
  if (__isInCJS) {
    return hasDefault ? resolvedDefault != null ? resolvedDefault : module2 : module2;
  }
  {
    if (hasDefault) {
      return resolvedDefault;
    }
    throw new Error("Default export not found.");
  }
}
const defaultCodeHighlightTheme = {
  light: "github-light",
  dark: "github-dark-dimmed"
};
const coerceTheme = (theme) => {
  if (typeof theme === "string") {
    return theme;
  }
  if ("tokenColors" in theme && theme.tokenColors) {
    return theme;
  }
  if ("settings" in theme) {
    return {
      ...theme,
      tokenColors: theme.settings
    };
  }
  return theme;
};
const isThemeConfig$1 = (theme) => {
  return typeof theme === "object" && theme !== null && ("light" in theme || "dark" in theme);
};
const codeMetaBraceToken = {
  left: "__mtr_code_meta_lbrace__",
  right: "__mtr_code_meta_rbrace__"
};
const escapeCodeMetaBraces = (meta) => {
  if (!meta.includes("{") && !meta.includes("}")) {
    return meta;
  }
  return meta.replace(/\{/g, codeMetaBraceToken.left).replace(/\}/g, codeMetaBraceToken.right);
};
const restoreCodeMetaBraces = (meta) => {
  if (!meta.includes(codeMetaBraceToken.left) && !meta.includes(codeMetaBraceToken.right)) {
    return meta;
  }
  return meta.split(codeMetaBraceToken.left).join("{").split(codeMetaBraceToken.right).join("}");
};
const protectCodeMeta = (node) => {
  if (!node || typeof node !== "object") {
    return;
  }
  if (node.type === "code" && typeof node.meta === "string") {
    node.meta = escapeCodeMetaBraces(node.meta);
  }
  if (Array.isArray(node.children)) {
    for (const child of node.children) {
      protectCodeMeta(child);
    }
  }
};
const remarkProtectCodeMeta = () => {
  return (tree) => {
    protectCodeMeta(tree);
  };
};
const resolveCodeHighlightTheme = (theme) => {
  var _a, _b;
  if (theme === void 0) {
    return defaultCodeHighlightTheme;
  }
  if (typeof theme === "string") {
    return coerceTheme(theme);
  }
  if (!isThemeConfig$1(theme)) {
    return coerceTheme(theme);
  }
  return {
    light: coerceTheme((_a = theme.light) != null ? _a : defaultCodeHighlightTheme.light),
    dark: coerceTheme((_b = theme.dark) != null ? _b : defaultCodeHighlightTheme.dark)
  };
};
const createMetaFilter = (lineNumbers) => {
  return (meta) => {
    const restored = restoreCodeMetaBraces(meta);
    const trimmed = restored.trim();
    if (!lineNumbers) {
      return trimmed;
    }
    if (/\bshowLineNumbers\b/.test(trimmed)) {
      return trimmed;
    }
    return trimmed.length > 0 ? `showLineNumbers ${trimmed}` : "showLineNumbers";
  };
};
const normalizeLanguageName = (value) => {
  const trimmed = value.trim();
  return trimmed.length > 0 ? trimmed : void 0;
};
const normalizeLanguageDefinitions = (definitions) => {
  if (!definitions || definitions.length === 0) {
    return [];
  }
  const normalized = /* @__PURE__ */ new Map();
  for (const definition of definitions) {
    const name = normalizeLanguageName(definition.name);
    if (!name) {
      continue;
    }
    if (normalized.has(name)) {
      normalized.delete(name);
    }
    normalized.set(name, definition);
  }
  return Array.from(normalized.values());
};
const normalizeLanguageAliases = (aliases) => {
  if (!aliases) {
    return void 0;
  }
  const normalized = {};
  for (const [key, value] of Object.entries(aliases)) {
    const from = normalizeLanguageName(key);
    const to = normalizeLanguageName(value);
    if (!from || !to) {
      continue;
    }
    normalized[from] = to;
  }
  return Object.keys(normalized).length > 0 ? normalized : void 0;
};
const buildCustomLanguageMap = (definitions) => {
  const customLanguages = /* @__PURE__ */ new Map();
  for (const definition of definitions) {
    const name = normalizeLanguageName(definition.name);
    if (!name) {
      continue;
    }
    if (customLanguages.has(name)) {
      customLanguages.delete(name);
    }
    customLanguages.set(name, definition);
    if (Array.isArray(definition.aliases)) {
      for (const alias of definition.aliases) {
        const normalized = normalizeLanguageName(alias);
        if (!normalized) {
          continue;
        }
        if (customLanguages.has(normalized)) {
          customLanguages.delete(normalized);
        }
        customLanguages.set(normalized, definition);
      }
    }
  }
  return customLanguages;
};
const resolveLanguageAlias = (name, aliases) => {
  if (!aliases) {
    return name;
  }
  let current = name;
  const seen = /* @__PURE__ */ new Set();
  while (aliases[current]) {
    if (seen.has(current)) {
      break;
    }
    seen.add(current);
    const next = aliases[current];
    if (!next || next === current) {
      break;
    }
    current = next;
  }
  return current;
};
const resolveLanguageName = (name, customLanguageMap, aliases) => {
  const normalized = normalizeLanguageName(name);
  if (!normalized) {
    return name;
  }
  const directCustom = customLanguageMap == null ? void 0 : customLanguageMap.get(normalized);
  if (directCustom) {
    return directCustom.name;
  }
  const resolvedAlias = resolveLanguageAlias(normalized, aliases);
  const aliasedCustom = customLanguageMap == null ? void 0 : customLanguageMap.get(resolvedAlias);
  if (aliasedCustom) {
    return aliasedCustom.name;
  }
  return resolvedAlias;
};
const resolveLanguageInput = (name, customLanguageMap, aliases) => {
  const normalized = normalizeLanguageName(name);
  if (!normalized) {
    return name;
  }
  const directCustom = customLanguageMap == null ? void 0 : customLanguageMap.get(normalized);
  if (directCustom) {
    return directCustom;
  }
  const resolvedAlias = resolveLanguageAlias(normalized, aliases);
  const aliasedCustom = customLanguageMap == null ? void 0 : customLanguageMap.get(resolvedAlias);
  if (aliasedCustom) {
    return aliasedCustom;
  }
  return resolvedAlias;
};
const createShikiHighlighter = (languageDefinitions, languageAliases) => {
  const normalizedDefinitions = normalizeLanguageDefinitions(languageDefinitions);
  const normalizedAliases = normalizeLanguageAliases(languageAliases);
  if (normalizedDefinitions.length === 0 && !normalizedAliases) {
    return void 0;
  }
  const customLanguageMap = normalizedDefinitions.length > 0 ? buildCustomLanguageMap(normalizedDefinitions) : void 0;
  return async (options) => {
    const { langAlias, langs, ...rest } = options;
    const mergedLangs = normalizedDefinitions.length > 0 ? [...langs, ...normalizedDefinitions] : langs;
    const mergedLangAlias = normalizedAliases ? { ...langAlias, ...normalizedAliases } : langAlias;
    const highlighter = await shiki.getSingletonHighlighter({
      ...rest,
      langs: mergedLangs,
      ...mergedLangAlias ? { langAlias: mergedLangAlias } : {}
    });
    if (customLanguageMap && customLanguageMap.size > 0 || normalizedAliases) {
      const loadLanguage = highlighter.loadLanguage.bind(highlighter);
      highlighter.loadLanguage = async (...langs2) => {
        const resolved = [];
        for (const lang of langs2) {
          if (typeof lang !== "string") {
            resolved.push(lang);
            continue;
          }
          resolved.push(
            resolveLanguageInput(lang, customLanguageMap, normalizedAliases)
          );
        }
        return loadLanguage(...resolved);
      };
      const codeToHtml = highlighter.codeToHtml.bind(
        highlighter
      );
      highlighter.codeToHtml = (code, options2) => {
        if (!options2 || typeof options2 !== "object") {
          return codeToHtml(code, options2);
        }
        const lang = options2.lang;
        if (typeof lang !== "string") {
          return codeToHtml(code, options2);
        }
        const resolvedLang = resolveLanguageName(
          lang,
          customLanguageMap,
          normalizedAliases
        );
        if (resolvedLang === lang) {
          return codeToHtml(code, options2);
        }
        return codeToHtml(code, { ...options2, lang: resolvedLang });
      };
    }
    return highlighter;
  };
};
const createCodeHighlightOptions = (codeHighlight) => {
  var _a;
  const options = {
    theme: resolveCodeHighlightTheme(codeHighlight == null ? void 0 : codeHighlight.theme),
    defaultLang: {
      block: (_a = codeHighlight == null ? void 0 : codeHighlight.defaultLanguage) != null ? _a : "text"
    },
    keepBackground: true,
    filterMetaString: createMetaFilter((codeHighlight == null ? void 0 : codeHighlight.lineNumbers) === true)
  };
  const highlighter = createShikiHighlighter(
    codeHighlight == null ? void 0 : codeHighlight.languageDefinitions,
    codeHighlight == null ? void 0 : codeHighlight.languageAliases
  );
  if (highlighter) {
    options.getHighlighter = highlighter;
  }
  return options;
};
const createCodeHighlightRehypePlugin = (codeHighlight) => {
  return [rehypePrettyCode, createCodeHighlightOptions(codeHighlight)];
};
const parseAttributes = (attrStr) => {
  const attributes = {};
  const content = attrStr.slice(1, -1).trim();
  const classMatches = content.match(/\.([a-zA-Z0-9_-]+)/g);
  if (classMatches) {
    const classes = classMatches.map((match) => match.slice(1));
    attributes.class = classes.join(" ");
  }
  const idMatch = content.match(/#([a-zA-Z0-9_-]+)/);
  if (idMatch && idMatch[1]) {
    attributes.id = idMatch[1];
  }
  const attrMatches = content.match(/([a-zA-Z0-9_-]+)=["']?([^"'\s]*)["']?/g);
  if (attrMatches) {
    for (const attr of attrMatches) {
      const [key, value] = attr.split("=");
      if (key && value && key !== "class" && key !== "id") {
        attributes[key] = value.replace(/["']/g, "");
      }
    }
  }
  return attributes;
};
const generateCodeBlockHTML = (node) => {
  const lang = node.lang ? ` class="language-${node.lang}"` : "";
  const code = node.value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
  return `<pre><code${lang}>${code}</code></pre>`;
};
const applyAttributes = (node, attributes) => {
  if (!node.data) {
    node.data = {};
  }
  if (!node.data.hProperties) {
    node.data.hProperties = {};
  }
  Object.entries(attributes).forEach(([key, value]) => {
    if (key === "class") {
      const existingClass = node.data.hProperties.class;
      if (existingClass) {
        if (typeof existingClass === "string" && existingClass.startsWith("language-")) {
          node.data.hProperties.class = `${existingClass} ${value}`;
        } else {
          node.data.hProperties.class = `${existingClass} ${value}`;
        }
      } else {
        node.data.hProperties.class = value;
      }
    } else {
      node.data.hProperties[key] = value;
    }
  });
};
const remarkAttr = () => {
  return (tree) => {
    unistUtilVisit.visit(tree, (node, index, parent) => {
      var _a, _b, _c, _d, _e, _f, _g, _h;
      if (node.type === "heading" && ((_a = node.children) == null ? void 0 : _a.length) > 0) {
        const lastChild = node.children[node.children.length - 1];
        if ((lastChild == null ? void 0 : lastChild.type) === "text" && lastChild.value) {
          const match = lastChild.value.match(/^(.*?)\s*(\{[^}]+\})\s*$/);
          if (match) {
            const cleanText = match[1].trim();
            const attrStr = match[2];
            const attributes = parseAttributes(attrStr);
            lastChild.value = cleanText;
            applyAttributes(node, attributes);
          }
        }
      }
      if (node.type === "paragraph" && ((_b = node.children) == null ? void 0 : _b.length) > 0) {
        const lastChild = node.children[node.children.length - 1];
        if ((lastChild == null ? void 0 : lastChild.type) === "text" && lastChild.value) {
          const match = lastChild.value.match(/^(.*?)\s*(\{[^}]+\})\s*$/);
          if (match) {
            const cleanText = match[1].trim();
            const attrStr = match[2];
            const attributes = parseAttributes(attrStr);
            lastChild.value = cleanText;
            applyAttributes(node, attributes);
          }
        }
      }
      if (node.type === "link" && parent && typeof index === "number") {
        const nextSibling = parent.children[index + 1];
        if ((nextSibling == null ? void 0 : nextSibling.type) === "text" && nextSibling.value) {
          const match = nextSibling.value.match(/^\s*(\{[^}]+\})/);
          if (match) {
            const attrStr = match[1];
            const attributes = parseAttributes(attrStr);
            applyAttributes(node, attributes);
            nextSibling.value = nextSibling.value.replace(match[0], "");
          }
        }
      }
      if (node.type === "image" && parent && typeof index === "number") {
        const nextSibling = parent.children[index + 1];
        if ((nextSibling == null ? void 0 : nextSibling.type) === "text" && nextSibling.value) {
          const match = nextSibling.value.match(/^\s*(\{[^}]+\})/);
          if (match) {
            const attrStr = match[1];
            const attributes = parseAttributes(attrStr);
            applyAttributes(node, attributes);
            nextSibling.value = nextSibling.value.replace(match[0], "");
          }
        }
      }
      if (node.type === "listItem" && ((_c = node.children) == null ? void 0 : _c.length) > 0) {
        const lastChild = node.children[node.children.length - 1];
        if ((lastChild == null ? void 0 : lastChild.type) === "paragraph" && ((_d = lastChild.children) == null ? void 0 : _d.length) > 0) {
          const lastTextNode = lastChild.children[lastChild.children.length - 1];
          if ((lastTextNode == null ? void 0 : lastTextNode.type) === "text" && lastTextNode.value) {
            const match = lastTextNode.value.match(/^(.*?)\s*(\{[^}]+\})\s*$/);
            if (match) {
              const cleanText = match[1].trim();
              const attrStr = match[2];
              const attributes = parseAttributes(attrStr);
              lastTextNode.value = cleanText;
              applyAttributes(node, attributes);
            }
          }
        }
      }
      if (node.type === "list" && parent && typeof index === "number") {
        const nextSibling = parent.children[index + 1];
        if ((nextSibling == null ? void 0 : nextSibling.type) === "paragraph" && ((_e = nextSibling.children) == null ? void 0 : _e.length) === 1) {
          const textNode = nextSibling.children[0];
          if ((textNode == null ? void 0 : textNode.type) === "text" && textNode.value) {
            const match = textNode.value.match(/^\s*(\{[^}]+\})\s*$/);
            if (match) {
              const attrStr = match[1];
              const attributes = parseAttributes(attrStr);
              applyAttributes(node, attributes);
              parent.children.splice(index + 1, 1);
            }
          }
        }
      }
      if (node.type === "blockquote" && ((_f = node.children) == null ? void 0 : _f.length) > 0) {
        const lastChild = node.children[node.children.length - 1];
        if ((lastChild == null ? void 0 : lastChild.type) === "paragraph" && ((_g = lastChild.children) == null ? void 0 : _g.length) > 0) {
          const lastTextNode = lastChild.children[lastChild.children.length - 1];
          if ((lastTextNode == null ? void 0 : lastTextNode.type) === "text" && lastTextNode.value) {
            const match = lastTextNode.value.match(/^(.*?)\s*(\{[^}]+\})\s*$/);
            if (match) {
              const cleanText = match[1].trim();
              const attrStr = match[2];
              const attributes = parseAttributes(attrStr);
              lastTextNode.value = cleanText;
              applyAttributes(node, attributes);
            }
          }
        }
      }
      if (node.type === "blockquote" && parent && typeof index === "number") {
        const nextSibling = parent.children[index + 1];
        if ((nextSibling == null ? void 0 : nextSibling.type) === "paragraph" && ((_h = nextSibling.children) == null ? void 0 : _h.length) === 1) {
          const textNode = nextSibling.children[0];
          if ((textNode == null ? void 0 : textNode.type) === "text" && textNode.value) {
            const match = textNode.value.match(/^\s*(\{[^}]+\})\s*$/);
            if (match) {
              const attrStr = match[1];
              const attributes = parseAttributes(attrStr);
              applyAttributes(node, attributes);
              parent.children.splice(index + 1, 1);
            }
          }
        }
      }
      if (node.type === "inlineCode" && parent && typeof index === "number") {
        const nextSibling = parent.children[index + 1];
        if ((nextSibling == null ? void 0 : nextSibling.type) === "text" && nextSibling.value) {
          const match = nextSibling.value.match(/^\s*(\{[^}]+\})/);
          if (match) {
            const attrStr = match[1];
            const attributes = parseAttributes(attrStr);
            applyAttributes(node, attributes);
            nextSibling.value = nextSibling.value.replace(match[0], "");
          }
        }
      }
      if (node.type === "code" && node.meta) {
        const match = node.meta.match(/\{([^}]+)\}/);
        if (match) {
          const attrStr = `{${match[1]}}`;
          const attributes = parseAttributes(attrStr);
          const wrapperDiv = {
            type: "html",
            value: `<div class="${attributes.class || ""}" ${Object.entries(
              attributes
            ).filter(([key]) => key !== "class").map(([key, value]) => `${key}="${value}"`).join(" ")}>${generateCodeBlockHTML(node)}</div>`
          };
          if (parent && typeof index === "number") {
            parent.children[index] = wrapperDiv;
          }
          node.meta = node.meta.replace(match[0], "").trim() || null;
        }
      }
    });
  };
};
const normalizeClassList = (value) => {
  if (Array.isArray(value)) {
    return value.filter((item) => typeof item === "string").flatMap((item) => item.split(/\s+/)).filter(Boolean);
  }
  if (typeof value === "string") {
    return value.split(/\s+/).filter(Boolean);
  }
  return [];
};
const mergeClassLists = (existing, extra) => {
  const merged = [];
  const seen = /* @__PURE__ */ new Set();
  for (const value of [...existing, ...extra]) {
    if (!seen.has(value)) {
      seen.add(value);
      merged.push(value);
    }
  }
  return merged;
};
const getNodeClassList = (node) => {
  var _a;
  if (!node.properties) {
    return [];
  }
  return normalizeClassList(
    (_a = node.properties.className) != null ? _a : node.properties.class
  );
};
const setNodeClassList = (node, classList) => {
  if (!node.properties) {
    node.properties = {};
  }
  if (classList.length === 0) {
    if ("className" in node.properties) {
      delete node.properties.className;
    }
    if ("class" in node.properties) {
      delete node.properties.class;
    }
    return;
  }
  node.properties.className = classList;
  if ("class" in node.properties) {
    delete node.properties.class;
  }
};
const hasLoadingProperty = (node) => {
  if (!node.properties) {
    return false;
  }
  return Object.prototype.hasOwnProperty.call(node.properties, "loading");
};
const collectImageNodes = (node) => {
  var _a;
  const images = [];
  const stack = [...(_a = node.children) != null ? _a : []];
  while (stack.length > 0) {
    const child = stack.pop();
    if (!child || child.type !== "element") {
      continue;
    }
    if (child.tagName === "img") {
      images.push(child);
      continue;
    }
    if (child.children && child.children.length > 0) {
      stack.push(...child.children);
    }
  }
  return images;
};
const rehypeResponsiveImages = (options = {}) => {
  const defaultOuterClassList = normalizeClassList(
    options.defaultOuterClassName
  );
  const applyLazyLoadingToImg = options.applyLazyLoadingToImg === true;
  return (tree) => {
    unistUtilVisit.visit(tree, "element", (node, _index, parent) => {
      if (node.tagName === "p") {
        const imageNodes = collectImageNodes(node);
        if (imageNodes.length > 0) {
          let imageClassList = [];
          for (const imageNode of imageNodes) {
            imageClassList = mergeClassLists(
              imageClassList,
              getNodeClassList(imageNode)
            );
          }
          const mergedClassList = mergeClassLists(
            getNodeClassList(node),
            mergeClassLists(imageClassList, defaultOuterClassList)
          );
          if (mergedClassList.length > 0) {
            setNodeClassList(node, mergedClassList);
          }
          for (const imageNode of imageNodes) {
            setNodeClassList(imageNode, []);
          }
        }
      }
      if (node.tagName === "img") {
        const responsiveStyles = htmlGenerator.generateResponsiveImageStyles();
        if (!node.properties) {
          node.properties = {};
        }
        const parentElement = parent && parent.type === "element" ? parent : void 0;
        const hasParentLoading = (parentElement == null ? void 0 : parentElement.tagName) === "p" && hasLoadingProperty(parentElement);
        if (applyLazyLoadingToImg && !hasLoadingProperty(node) && !hasParentLoading) {
          node.properties.loading = "lazy";
        }
        const existingStyle = node.properties.style || "";
        const combinedStyles = existingStyle ? `${existingStyle}; ${responsiveStyles}` : responsiveStyles;
        node.properties.style = combinedStyles;
      }
    });
  };
};
const urlAttributePattern = /\b(?:href|src|srcset|poster|data|action|formaction|cite|xlink:href)\s*=/i;
const urlAttributes = /* @__PURE__ */ new Set([
  "href",
  "src",
  "srcset",
  "poster",
  "data",
  "action",
  "formaction",
  "cite",
  "xlink:href"
]);
const rawTextTags = /* @__PURE__ */ new Set(["script", "style", "textarea", "title"]);
const isWhitespace = (char) => char === " " || char === "	" || char === "\n" || char === "\r" || char === "\f";
const isNameChar = (char) => /[A-Za-z0-9:_-]/.test(char);
const charAt = (value, index) => value.charAt(index);
const rewriteSrcset = (value, tagName, resolveUrl) => {
  let i = 0;
  const length = value.length;
  let output = "";
  let changed = false;
  while (i < length) {
    const leadingStart = i;
    while (i < length && isWhitespace(charAt(value, i))) {
      i += 1;
    }
    output += value.slice(leadingStart, i);
    if (i >= length) {
      break;
    }
    let quote = "";
    let url = "";
    if (charAt(value, i) === '"' || charAt(value, i) === "'") {
      quote = charAt(value, i);
      i += 1;
      const urlStart = i;
      while (i < length && charAt(value, i) !== quote) {
        i += 1;
      }
      url = value.slice(urlStart, i);
      if (i < length) {
        i += 1;
      }
    } else {
      const urlStart = i;
      while (i < length && !isWhitespace(charAt(value, i)) && charAt(value, i) !== ",") {
        i += 1;
      }
      url = value.slice(urlStart, i);
    }
    if (url.length === 0) {
      if (i < length && charAt(value, i) === ",") {
        output += ",";
        i += 1;
        continue;
      }
      break;
    }
    const updated = resolveUrl(url, {
      kind: "html",
      tagName,
      attrName: "srcset"
    });
    if (updated !== url) {
      changed = true;
    }
    if (quote) {
      output += `${quote}${updated}${quote}`;
    } else {
      output += updated;
    }
    const descriptorStart = i;
    while (i < length && charAt(value, i) !== ",") {
      i += 1;
    }
    output += value.slice(descriptorStart, i);
    if (i < length && charAt(value, i) === ",") {
      output += ",";
      i += 1;
    }
  }
  return changed ? output : value;
};
const rewriteHtmlUrls = (html, resolveUrl) => {
  if (!urlAttributePattern.test(html)) {
    return html;
  }
  const length = html.length;
  let lowerHtml = null;
  let cursor = 0;
  let i = 0;
  let changed = false;
  const parts = [];
  const getLowerHtml = () => {
    if (!lowerHtml) {
      lowerHtml = html.toLowerCase();
    }
    return lowerHtml;
  };
  const commit = (end) => {
    if (end <= cursor) {
      return;
    }
    parts.push(html.slice(cursor, end));
    cursor = end;
  };
  while (i < length) {
    const ltIndex = html.indexOf("<", i);
    if (ltIndex < 0) {
      break;
    }
    i = ltIndex + 1;
    if (i >= length) {
      break;
    }
    const nextChar = charAt(html, i);
    if (nextChar === "!") {
      if (html.startsWith("!--", i)) {
        const end2 = html.indexOf("-->", i + 3);
        i = end2 >= 0 ? end2 + 3 : length;
        continue;
      }
      const end = html.indexOf(">", i + 1);
      if (end < 0) {
        break;
      }
      i = end + 1;
      continue;
    }
    if (nextChar === "?") {
      const end = html.indexOf("?>", i + 1);
      if (end < 0) {
        break;
      }
      i = end + 2;
      continue;
    }
    let isClosing = false;
    if (nextChar === "/") {
      isClosing = true;
      i += 1;
    }
    while (i < length && isWhitespace(charAt(html, i))) {
      i += 1;
    }
    const tagStart = i;
    while (i < length && isNameChar(charAt(html, i))) {
      i += 1;
    }
    if (i === tagStart) {
      continue;
    }
    const tagName = html.slice(tagStart, i);
    const tagNameLower = tagName.toLowerCase();
    if (isClosing) {
      const end = html.indexOf(">", i);
      if (end < 0) {
        break;
      }
      i = end + 1;
      continue;
    }
    let isSelfClosing = false;
    while (i < length) {
      const char = charAt(html, i);
      if (char === ">") {
        i += 1;
        break;
      }
      if (char === "/" && charAt(html, i + 1) === ">") {
        isSelfClosing = true;
        i += 2;
        break;
      }
      if (isWhitespace(char)) {
        i += 1;
        continue;
      }
      const attrStart = i;
      while (i < length && isNameChar(charAt(html, i))) {
        i += 1;
      }
      if (i === attrStart) {
        i += 1;
        continue;
      }
      const attrName = html.slice(attrStart, i);
      const attrNameLower = attrName.toLowerCase();
      while (i < length && isWhitespace(charAt(html, i))) {
        i += 1;
      }
      if (charAt(html, i) !== "=") {
        continue;
      }
      i += 1;
      while (i < length && isWhitespace(charAt(html, i))) {
        i += 1;
      }
      if (i >= length) {
        break;
      }
      let quote = "";
      let valueStart = i;
      let valueEnd = i;
      if (charAt(html, i) === '"' || charAt(html, i) === "'") {
        quote = charAt(html, i);
        valueStart = i + 1;
        const closing = html.indexOf(quote, valueStart);
        if (closing < 0) {
          return html;
        }
        valueEnd = closing;
        i = closing + 1;
      } else {
        valueStart = i;
        while (i < length && !isWhitespace(charAt(html, i)) && charAt(html, i) !== ">" && !(charAt(html, i) === "/" && charAt(html, i + 1) === ">")) {
          i += 1;
        }
        valueEnd = i;
      }
      if (!urlAttributes.has(attrNameLower)) {
        continue;
      }
      const original = html.slice(valueStart, valueEnd);
      let updated = original;
      if (attrNameLower === "srcset") {
        updated = rewriteSrcset(original, tagNameLower, resolveUrl);
      } else {
        updated = resolveUrl(original, {
          kind: "html",
          tagName: tagNameLower,
          attrName: attrNameLower
        });
      }
      if (updated !== original) {
        changed = true;
        commit(valueStart);
        parts.push(updated);
        cursor = valueEnd;
      }
    }
    if (!isSelfClosing && rawTextTags.has(tagNameLower)) {
      const lower = getLowerHtml();
      const closeIndex = lower.indexOf(`</${tagNameLower}`, i);
      if (closeIndex < 0) {
        break;
      }
      const closeEnd = html.indexOf(">", closeIndex + 2);
      if (closeEnd < 0) {
        break;
      }
      i = closeEnd + 1;
    }
  }
  if (!changed) {
    return html;
  }
  parts.push(html.slice(cursor));
  return parts.join("");
};
const extractHeadingText = (node) => {
  if (!node) {
    return "";
  }
  if ("value" in node && typeof node.value === "string") {
    return node.value;
  }
  if ("children" in node && Array.isArray(node.children)) {
    return node.children.map((child) => extractHeadingText(child)).join("");
  }
  return "";
};
const clampHeadingLevel = (level) => {
  if (!Number.isFinite(level)) {
    return 1;
  }
  const normalized = Math.trunc(level);
  return Math.min(6, Math.max(1, normalized));
};
const resolveHeadingBaseLevel = (baseLevel) => {
  return clampHeadingLevel(baseLevel != null ? baseLevel : 1);
};
const remarkApplyTitleFromH1 = (options) => {
  const {
    frontmatter,
    hasTitle,
    transform,
    allowTitleWrite,
    headingBaseLevel,
    onHeadingApplied
  } = options;
  const shouldRemoveHeading = transform === "extractAndRemove";
  const baseLevel = resolveHeadingBaseLevel(headingBaseLevel);
  const offset = baseLevel - 1;
  return (tree) => {
    var _a, _b;
    if (!frontmatter || !tree.children || tree.children.length === 0) {
      return;
    }
    let index = 0;
    for (; index < tree.children.length; index++) {
      const node = tree.children[index];
      if (node.type === "heading") {
        const heading = node;
        const outputLevel = clampHeadingLevel(heading.depth + offset);
        if (outputLevel !== baseLevel) {
          break;
        }
        const nextNode = tree.children[index + 1];
        const nextNodeStartOffset = nextNode && nextNode.position ? (_a = nextNode.position.start) == null ? void 0 : _a.offset : void 0;
        const headingText = extractHeadingText(heading).trim();
        if (shouldRemoveHeading) {
          tree.children.splice(index, 1);
        }
        let titleWritten = false;
        if (!hasTitle && allowTitleWrite && headingText.length > 0) {
          frontmatter.title = headingText;
          titleWritten = true;
        }
        if (onHeadingApplied) {
          onHeadingApplied({
            headingText,
            position: heading.position,
            nextNodeStartOffset,
            titleWritten,
            headingRemoved: shouldRemoveHeading
          });
        }
        break;
      }
      if (node.type === "thematicBreak" || node.type === "html" && typeof node.value === "string" && node.value.trim() === "") {
        continue;
      }
      if (node.type === "paragraph" && ((_b = node.children) == null ? void 0 : _b.length) === 0) {
        continue;
      }
      break;
    }
  };
};
const remarkParsePlugin$1 = __resolveDefaultExport$1(__screwUpDefaultImportModule0__namespace$1);
globalThis.__screwUpIsInCJS_76295409924d = true;
function __resolveDefaultExport$1(module2, isESM) {
  const __isInCJS = typeof globalThis !== "undefined" && globalThis.__screwUpIsInCJS_76295409924d === true;
  const maybe = module2;
  const hasDefault = !!(maybe && typeof maybe === "object" && "default" in maybe);
  const unwrapNamespaceDefault = (value) => {
    if (!value || typeof value !== "object") {
      return value;
    }
    const inner = value;
    const isModule = inner.__esModule === true || typeof Symbol !== "undefined" && inner[Symbol.toStringTag] === "Module";
    if (isModule && "default" in inner) {
      return inner.default;
    }
    return value;
  };
  const resolvedDefault = hasDefault ? unwrapNamespaceDefault(maybe.default) : void 0;
  if (__isInCJS) {
    return hasDefault ? resolvedDefault != null ? resolvedDefault : module2 : module2;
  }
  {
    if (hasDefault) {
      return resolvedDefault;
    }
    throw new Error("Default export not found.");
  }
}
const applyTitleFromH1 = (markdownContent, frontmatter, options) => {
  const { transform, allowTitleWrite, headingBaseLevel } = options;
  if (transform === "none") {
    return {
      content: markdownContent,
      headingRemoved: false,
      titleWritten: false
    };
  }
  const hasTitle = frontmatter !== void 0 && frontmatter !== null && Object.prototype.hasOwnProperty.call(frontmatter, "title") && frontmatter.title !== void 0 && frontmatter.title !== null;
  let headingRemoved = false;
  let titleWritten = false;
  let removeStartOffset;
  let removeEndOffset;
  const runner = unified.unified().use(remarkParsePlugin$1).use(remarkApplyTitleFromH1, {
    frontmatter,
    hasTitle,
    transform,
    allowTitleWrite,
    headingBaseLevel,
    onHeadingApplied: (info) => {
      var _a, _b, _c, _d, _e, _f;
      headingRemoved = info.headingRemoved;
      titleWritten = info.titleWritten;
      if (!info.headingRemoved) {
        return;
      }
      const startOffset = (_c = (_b = (_a = info.position) == null ? void 0 : _a.start) == null ? void 0 : _b.offset) != null ? _c : 0;
      let endOffset = (_f = (_e = (_d = info.position) == null ? void 0 : _d.end) == null ? void 0 : _e.offset) != null ? _f : startOffset;
      if (typeof info.nextNodeStartOffset === "number" && info.nextNodeStartOffset >= endOffset) {
        endOffset = info.nextNodeStartOffset;
      } else {
        endOffset = markdownContent.length;
      }
      removeStartOffset = startOffset;
      removeEndOffset = endOffset;
    }
  });
  const tree = runner.parse(markdownContent);
  runner.runSync(tree);
  if (headingRemoved && removeStartOffset !== void 0 && removeEndOffset !== void 0) {
    const updatedContent = markdownContent.slice(0, removeStartOffset) + markdownContent.slice(removeEndOffset);
    return {
      content: updatedContent,
      headingRemoved: true,
      titleWritten
    };
  }
  return {
    content: markdownContent,
    headingRemoved: false,
    titleWritten: false
  };
};
const beautify = __resolveDefaultExport(__screwUpDefaultImportModule0$2, false);
const rehypeStringifyPlugin = __resolveDefaultExport(__screwUpDefaultImportModule1__namespace, true);
const remarkGfmPlugin = __resolveDefaultExport(__screwUpDefaultImportModule2__namespace, true);
const remarkParsePlugin = __resolveDefaultExport(__screwUpDefaultImportModule0__namespace$1, true);
const remarkRehypePlugin = __resolveDefaultExport(__screwUpDefaultImportModule4__namespace, true);
globalThis.__screwUpIsInCJS_f274938a069f = true;
function __resolveDefaultExport(module2, isESM) {
  const __isInCJS = typeof globalThis !== "undefined" && globalThis.__screwUpIsInCJS_f274938a069f === true;
  const maybe = module2;
  const hasDefault = !!(maybe && typeof maybe === "object" && "default" in maybe);
  const unwrapNamespaceDefault = (value) => {
    if (!value || typeof value !== "object") {
      return value;
    }
    const inner = value;
    const isModule = inner.__esModule === true || typeof Symbol !== "undefined" && inner[Symbol.toStringTag] === "Module";
    if (isModule && "default" in inner) {
      return inner.default;
    }
    return value;
  };
  const resolvedDefault = hasDefault ? unwrapNamespaceDefault(maybe.default) : void 0;
  if (__isInCJS) {
    return hasDefault ? resolvedDefault != null ? resolvedDefault : module2 : module2;
  }
  if (isESM) {
    if (hasDefault) {
      return resolvedDefault;
    }
    throw new Error("Default export not found.");
  }
  return hasDefault ? resolvedDefault != null ? resolvedDefault : module2 : module2;
}
const { html: beautifyHtml } = beautify;
const defaultHtmlOptions = {
  indent_size: 2,
  indent_char: " ",
  max_preserve_newlines: 2,
  preserve_newlines: true,
  end_with_newline: false,
  wrap_line_length: 0,
  indent_inner_html: true,
  indent_empty_lines: false,
  unformatted: ["code", "pre", "textarea"],
  content_unformatted: ["pre", "code", "textarea"],
  extra_liners: []
};
const normalizeFrontmatterValue = (value) => {
  if (value instanceof Date) {
    return value.toISOString();
  }
  if (Array.isArray(value)) {
    return value.map(normalizeFrontmatterValue);
  }
  if (value && typeof value === "object") {
    const entries = Object.entries(value).filter(([, inner]) => inner !== void 0).sort(([a], [b]) => a < b ? -1 : a > b ? 1 : 0);
    const normalized = {};
    for (const [key, inner] of entries) {
      normalized[key] = normalizeFrontmatterValue(inner);
    }
    return normalized;
  }
  return value;
};
const snapshotFrontmatter = (data) => {
  return JSON.stringify(normalizeFrontmatterValue(data != null ? data : {}));
};
const generateHierarchicalId = (prefix, numbers) => {
  return `${prefix}-${numbers.join("-")}`;
};
const buildHierarchicalNumbers = (headings, headingBaseLevel) => {
  const result = [];
  const stack = [];
  const baseLevel = resolveHeadingBaseLevel(headingBaseLevel);
  for (const heading of headings) {
    const level = Math.max(1, heading.level - baseLevel + 1);
    while (stack.length > level) {
      stack.pop();
    }
    while (stack.length < level) {
      stack.push(0);
    }
    const currentIndex = level - 1;
    stack[currentIndex] = (stack[currentIndex] || 0) + 1;
    const numbers = stack.slice(0, level);
    result.push({
      level: heading.level,
      text: heading.text,
      numbers
    });
  }
  return result;
};
const generateHeadingIdInternal = (prefix, text, fallbackId) => {
  const result = utils.generateHeadingId(text);
  if (result !== void 0) {
    return `${prefix}-${result}`;
  } else {
    return fallbackId();
  }
};
const buildHeadingTree = (headings) => {
  const tree = [];
  const stack = [];
  for (const heading of headings) {
    const node = {
      level: heading.level,
      text: heading.text,
      id: heading.id,
      children: []
    };
    while (stack.length > 0) {
      const lastItem = stack[stack.length - 1];
      if (lastItem && lastItem.level >= heading.level) {
        stack.pop();
      } else {
        break;
      }
    }
    if (stack.length === 0) {
      tree.push(node);
    } else {
      const parent = stack[stack.length - 1];
      if (parent) {
        parent.children.push(node);
      }
    }
    stack.push(node);
  }
  return tree;
};
const createMarkdownProcessor = (options) => {
  const { plugins = [], logger = getNoOpLogger(), fetcher } = options;
  const pluginsMap = /* @__PURE__ */ new Map();
  for (const plugin of plugins) {
    if (pluginsMap.has(plugin.name)) {
      throw new Error(
        `Plugin with name '${plugin.name}' is already registered`
      );
    }
    pluginsMap.set(plugin.name, plugin);
  }
  const processBlock = async (language, content, context) => {
    const plugin = pluginsMap.get(language);
    if (!plugin) {
      return `<pre><code class="language-${language}">${htmlGenerator.escapeHtml(content)}</code></pre>`;
    }
    return await plugin.processBlock(content, context);
  };
  const createCustomBlockPlugin = (frontmatter, signal, getUniqueId) => {
    const context = {
      logger,
      signal,
      frontmatter,
      getUniqueId,
      fetcher
    };
    return () => {
      return async (tree) => {
        const promises = [];
        unistUtilVisit.visit(
          tree,
          "code",
          (node, index, parent) => {
            if (!node.lang || !pluginsMap.has(node.lang)) {
              return;
            }
            const promise = (async () => {
              const processedHtml = await processBlock(
                node.lang,
                node.value,
                context
              );
              const htmlNode = {
                type: "html",
                value: processedHtml
              };
              if (parent && typeof index === "number") {
                parent.children[index] = htmlNode;
              }
            })();
            promises.push(promise);
          }
        );
        await Promise.all(promises);
      };
    };
  };
  const createResolveUrlPlugin = (resolveUrl) => {
    return () => {
      return (tree) => {
        unistUtilVisit.visit(tree, (node) => {
          if (!node) {
            return;
          }
          if (node.type === "link" && typeof node.url === "string") {
            node.url = resolveUrl(node.url, { kind: "link" });
            return;
          }
          if (node.type === "image" && typeof node.url === "string") {
            node.url = resolveUrl(node.url, { kind: "image" });
            return;
          }
          if (node.type === "definition" && typeof node.url === "string") {
            node.url = resolveUrl(node.url, { kind: "definition" });
            return;
          }
          if (node.type === "html" && typeof node.value === "string") {
            node.value = rewriteHtmlUrls(node.value, resolveUrl);
          }
        });
      };
    };
  };
  const createLinkTargetPlugin = (target, rel) => {
    return () => {
      return (tree) => {
        unistUtilVisit.visit(tree, "link", (node) => {
          if (!node) {
            return;
          }
          if (!node.data) {
            node.data = {};
          }
          if (!node.data.hProperties) {
            node.data.hProperties = {};
          }
          const props = node.data.hProperties;
          if (props.target !== void 0) {
            return;
          }
          props.target = target;
          if (props.rel !== void 0) {
            return;
          }
          const resolvedRel = rel != null ? rel : target === "_blank" ? "noopener noreferrer" : void 0;
          if (resolvedRel !== void 0) {
            props.rel = resolvedRel;
          }
        });
      };
    };
  };
  const createHeadingTreePlugin = (headingTree, generateId, useHierarchicalId = false, useContentBasedId = false, uniqueIdPrefix = "", headingBaseLevel = 1) => {
    return () => {
      return (tree) => {
        const headings = [];
        const baseLevel = resolveHeadingBaseLevel(headingBaseLevel);
        const offset = baseLevel - 1;
        const resolveAdjustedLevel = (depth) => {
          return clampHeadingLevel(depth + offset);
        };
        const headingNodes = [];
        unistUtilVisit.visit(tree, "heading", (node) => {
          if (node.depth >= 1 && node.depth <= 6) {
            const adjustedLevel = resolveAdjustedLevel(node.depth);
            if (!node.data) {
              node.data = {};
            }
            node.data.hName = `h${adjustedLevel}`;
            headingNodes.push({ node, level: adjustedLevel });
          }
        });
        let hierarchicalNumbers = [];
        if (useHierarchicalId) {
          const basicHeadings = headingNodes.map(({ node, level }) => ({
            level,
            text: extractHeadingText(node)
          }));
          hierarchicalNumbers = buildHierarchicalNumbers(
            basicHeadings,
            baseLevel
          );
        }
        const parentContentIds = [];
        headingNodes.forEach(({ node, level }, index) => {
          var _a, _b;
          const headingText = extractHeadingText(node);
          const existingId = (_b = (_a = node.data) == null ? void 0 : _a.hProperties) == null ? void 0 : _b.id;
          let headingId;
          if (existingId) {
            headingId = existingId;
          } else if (useHierarchicalId && useContentBasedId) {
            const contentBasedId = generateHeadingIdInternal(
              uniqueIdPrefix,
              headingText,
              () => generateId(headingText)
            ).replace(`${uniqueIdPrefix}-`, "");
            while (parentContentIds.length > 0) {
              const lastParent = parentContentIds[parentContentIds.length - 1];
              if (lastParent && lastParent.level >= level) {
                parentContentIds.pop();
              } else {
                break;
              }
            }
            const parentParts = parentContentIds.map((p) => p.contentId);
            const hierarchicalContentId = parentParts.length > 0 ? `${uniqueIdPrefix}-${parentParts.join("-")}-${contentBasedId}` : `${uniqueIdPrefix}-${contentBasedId}`;
            headingId = hierarchicalContentId;
            parentContentIds.push({
              level,
              contentId: contentBasedId
            });
          } else if (useHierarchicalId && index < hierarchicalNumbers.length) {
            const hierarchicalData = hierarchicalNumbers[index];
            if (hierarchicalData) {
              headingId = generateHierarchicalId(
                uniqueIdPrefix,
                hierarchicalData.numbers
              );
            } else {
              headingId = generateId(headingText);
            }
          } else {
            headingId = generateId(headingText);
          }
          if (!node.data) {
            node.data = {};
          }
          if (!node.data.hProperties) {
            node.data.hProperties = {};
          }
          if (!existingId) {
            node.data.hProperties.id = headingId;
          }
          headings.push({
            level,
            text: headingText,
            id: headingId
          });
        });
        const builtTree = buildHeadingTree(headings);
        headingTree.push(...builtTree);
      };
    };
  };
  const handleProcessingError = (error) => {
    logger.error(
      `Failed to process markdown: ${error instanceof Error ? error.message : "Unknown error"}`
    );
    throw new Error(
      `Failed to process markdown: ${error instanceof Error ? error.message : "Unknown error"}`
    );
  };
  const processCore = async (markdown, uniqueIdPrefix, options2, frontmatter, content, changed) => {
    const {
      signal,
      useContentStringHeaderId = false,
      useHierarchicalHeadingId = true,
      headingBaseLevel,
      defaultImageOuterClassName,
      applyLazyLoadingToImg = false,
      codeHighlight,
      resolveUrl,
      linkTarget,
      linkRel,
      advancedOptions
    } = options2 != null ? options2 : {};
    const resolvedHeadingBaseLevel = resolveHeadingBaseLevel(headingBaseLevel);
    const hasCodeHighlight = codeHighlight !== void 0;
    const {
      allowDangerousHtml = true,
      htmlOptions = defaultHtmlOptions,
      gfmOptions = {},
      remarkPlugins = [],
      rehypePlugins = []
    } = advancedOptions || {};
    const headingTree = [];
    let idCounter = 0;
    const getUniqueId = () => {
      const id = `${uniqueIdPrefix}-${++idCounter}`;
      return id;
    };
    let processor0 = unified.unified().use(remarkParsePlugin);
    if (remarkPlugins) {
      for (const plugin of remarkPlugins) {
        if (Array.isArray(plugin)) {
          processor0 = processor0.use(plugin[0], plugin[1]);
        } else {
          processor0 = processor0.use(plugin);
        }
      }
    }
    if (hasCodeHighlight) {
      processor0 = processor0.use(remarkProtectCodeMeta);
    }
    const responsiveImageOptions = defaultImageOuterClassName === void 0 && !applyLazyLoadingToImg ? void 0 : {
      defaultOuterClassName: defaultImageOuterClassName,
      applyLazyLoadingToImg
    };
    const normalizedLinkTarget = typeof linkTarget === "string" && linkTarget.trim().length > 0 ? linkTarget : void 0;
    const normalizedLinkRel = typeof linkRel === "string" && linkRel.trim().length > 0 ? linkRel : void 0;
    let processor = processor0.use(remarkGfmPlugin, gfmOptions).use(remarkAttr);
    if (normalizedLinkTarget) {
      processor = processor.use(
        createLinkTargetPlugin(normalizedLinkTarget, normalizedLinkRel)
      );
    }
    processor = processor.use(
      createHeadingTreePlugin(
        headingTree,
        useContentStringHeaderId ? (text) => generateHeadingIdInternal(uniqueIdPrefix, text, getUniqueId) : getUniqueId,
        useHierarchicalHeadingId,
        useContentStringHeaderId,
        uniqueIdPrefix,
        resolvedHeadingBaseLevel
      )
    ).use(createCustomBlockPlugin(frontmatter, signal, getUniqueId));
    if (resolveUrl) {
      processor = processor.use(createResolveUrlPlugin(resolveUrl));
    }
    processor = processor.use(remarkRehypePlugin, { allowDangerousHtml }).use(rehypeResponsiveImages, responsiveImageOptions).use(rehypeStringifyPlugin, { allowDangerousHtml });
    if (hasCodeHighlight) {
      const [plugin, options3] = createCodeHighlightRehypePlugin(codeHighlight);
      processor = processor.use(plugin, options3);
    }
    if (rehypePlugins) {
      for (const plugin of rehypePlugins) {
        if (Array.isArray(plugin)) {
          processor = processor.use(plugin[0], plugin[1]);
        } else {
          processor = processor.use(plugin);
        }
      }
    }
    const result = await processor.process(content);
    const rehypedHtml = String(result);
    const formattedHtml = beautifyHtml(rehypedHtml, htmlOptions);
    const composeMarkdown = () => {
      if (!changed) {
        return markdown;
      }
      return composeMarkdownFromParts(frontmatter, content);
    };
    return {
      html: formattedHtml,
      frontmatter,
      changed,
      headingTree,
      composeMarkdown,
      uniqueIdPrefix
    };
  };
  const process = async (markdown, uniqueIdPrefix, options2 = {}) => {
    var _a;
    try {
      const { data: parsedFrontmatter, content } = parseFrontmatter(markdown);
      const resolvedHeadingBaseLevel = resolveHeadingBaseLevel(
        options2.headingBaseLevel
      );
      const titleTransform = (_a = options2.headerTitleTransform) != null ? _a : "extractAndRemove";
      let workingContent = content;
      let contentChanged = false;
      let frontmatterChanged = false;
      if (titleTransform !== "none") {
        const h1Result = applyTitleFromH1(workingContent, parsedFrontmatter, {
          allowTitleWrite: true,
          transform: titleTransform,
          headingBaseLevel: resolvedHeadingBaseLevel
        });
        workingContent = h1Result.content;
        contentChanged = h1Result.headingRemoved;
        frontmatterChanged = h1Result.titleWritten;
      }
      const changed = contentChanged || frontmatterChanged;
      const normalizedOptions = {
        ...options2,
        headingBaseLevel: resolvedHeadingBaseLevel
      };
      return await processCore(
        markdown,
        uniqueIdPrefix,
        normalizedOptions,
        parsedFrontmatter,
        workingContent,
        changed
      );
    } catch (error) {
      return handleProcessingError(error);
    }
  };
  const processWithFrontmatterTransform = async (markdown, uniqueIdPrefix, options2) => {
    try {
      const { data: parsedFrontmatter, content } = parseFrontmatter(markdown);
      const originalSnapshot = snapshotFrontmatter(parsedFrontmatter);
      const preContext = {
        originalFrontmatter: parsedFrontmatter,
        markdownContent: content,
        uniqueIdPrefix
      };
      const transformed = await options2.preTransform(preContext);
      if (transformed === void 0) {
        return void 0;
      }
      const {
        frontmatter,
        uniqueIdPrefix: overrideUniqueIdPrefix,
        headerTitleTransform
      } = transformed;
      const nextUniqueIdPrefix = overrideUniqueIdPrefix != null ? overrideUniqueIdPrefix : uniqueIdPrefix;
      const resolvedHeadingBaseLevel = resolveHeadingBaseLevel(
        options2.headingBaseLevel
      );
      const titleTransform = headerTitleTransform != null ? headerTitleTransform : "extractAndRemove";
      let workingContent = content;
      let contentChanged = false;
      if (titleTransform !== "none") {
        const h1Result = applyTitleFromH1(workingContent, frontmatter, {
          allowTitleWrite: true,
          transform: titleTransform,
          headingBaseLevel: resolvedHeadingBaseLevel
        });
        workingContent = h1Result.content;
        contentChanged = h1Result.headingRemoved || contentChanged;
      }
      const preSnapshot = snapshotFrontmatter(frontmatter);
      const preChanged = preSnapshot !== originalSnapshot || contentChanged;
      const normalizedOptions = {
        ...options2,
        headingBaseLevel: resolvedHeadingBaseLevel
      };
      const baseResult = await processCore(
        markdown,
        nextUniqueIdPrefix,
        normalizedOptions,
        frontmatter,
        workingContent,
        preChanged
      );
      let finalFrontmatter = baseResult.frontmatter;
      if (options2.postTransform) {
        const postContext = {
          frontmatter: finalFrontmatter,
          headingTree: baseResult.headingTree
        };
        const postTransformed = await options2.postTransform(postContext);
        finalFrontmatter = postTransformed;
      }
      const finalSnapshot = snapshotFrontmatter(finalFrontmatter);
      const finalChanged = finalSnapshot !== originalSnapshot || contentChanged;
      const composeMarkdown = () => {
        if (!finalChanged) {
          return markdown;
        }
        return composeMarkdownFromParts(finalFrontmatter, workingContent);
      };
      return {
        html: baseResult.html,
        frontmatter: finalFrontmatter,
        changed: finalChanged,
        headingTree: baseResult.headingTree,
        composeMarkdown,
        uniqueIdPrefix: baseResult.uniqueIdPrefix
      };
    } catch (error) {
      return handleProcessingError(error);
    }
  };
  return {
    process,
    processWithFrontmatterTransform
  };
};
const createOEmbedPlugin = (providerList, options = {}) => {
  const { maxRedirects = 5, timeoutEachRedirect = 1e4 } = options;
  const processBlock = async (content, context) => {
    const url = content.trim();
    if (!htmlGenerator.isValidUrl(url)) {
      throw new Error(`Invalid URL: ${url}`);
    }
    try {
      const oembedData = await htmlGenerator.fetchOEmbedData(
        url,
        maxRedirects,
        timeoutEachRedirect,
        context,
        providerList
      );
      return htmlGenerator.generateHtml(oembedData, url, options);
    } catch (error) {
      context.logger.warn("oEmbed fetch failed for URL:", url, error);
      const errorInfo = htmlGenerator$1.formatErrorInfo(error);
      if (utils.isBrowser()) {
        return htmlGenerator.generateFallbackHtml(url, errorInfo);
      }
      return htmlGenerator.generateFallbackHtml(url, errorInfo);
    }
  };
  return {
    name: "oembed",
    processBlock
  };
};
const createCardPlugin = (options = {}) => {
  const processBlock = async (content, context) => {
    const url = content.trim();
    if (!htmlGenerator$1.isValidUrl(url)) {
      throw new Error(`Invalid URL: ${url}`);
    }
    if (options.oembedFallback) {
      try {
        const oembedHtml = await options.oembedFallback.render(url, context);
        if (typeof oembedHtml === "string") {
          return oembedHtml;
        }
      } catch (error) {
        context.logger.warn(
          "Card plugin oEmbed fallback failed for URL:",
          url,
          error
        );
      }
    }
    try {
      const metadata = await htmlGenerator$1.fetchMetadata(url, options, context);
      return htmlGenerator$1.generateCardHtml(metadata, url, options);
    } catch (error) {
      context.logger.warn("Card plugin fetch failed for URL:", url, error);
      const errorInfo = htmlGenerator$1.formatErrorInfo(error);
      if (utils.isBrowser()) {
        return htmlGenerator$1.generateFallbackHtml(url, errorInfo);
      }
      return htmlGenerator$1.generateFallbackHtml(url, errorInfo);
    }
  };
  return {
    name: "card",
    processBlock
  };
};
const createMermaidPlugin = (options = {}) => {
  const { className = "mermaid", includeId = true } = options;
  const processBlock = async (content, context) => {
    const trimmedContent = content.trim();
    if (!trimmedContent) {
      context.logger.warn("Mermaid plugin received empty content");
      const idAttribute2 = includeId ? ` id="${context.getUniqueId()}"` : "";
      return `<div class="${className}-wrapper">
  <style>
    .${className}-wrapper .${className} > svg {
      width: auto !important;
      height: auto !important;
      max-width: none !important;
    }
  </style>
  <div class="${className}"${idAttribute2}><!-- Empty mermaid content --></div>
</div>`;
    }
    const idAttribute = includeId ? ` id="${context.getUniqueId()}"` : "";
    const escapedContent = trimmedContent.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
    const html = `<div class="${className}-wrapper">
  <style>
    .${className}-wrapper .${className} > svg {
      width: auto !important;
      height: auto !important;
      max-width: none !important;
    }
  </style>
  <div class="${className}"${idAttribute}>${escapedContent}</div>
</div>`;
    context.logger.debug("Mermaid plugin processed content:", {
      contentLength: trimmedContent.length,
      className,
      includeId
    });
    return html;
  };
  return {
    name: "mermaid",
    processBlock
  };
};
let cachedModule;
let modulePromise;
const loadBeautifulMermaid = async () => {
  if (cachedModule) {
    return cachedModule;
  }
  if (!modulePromise) {
    modulePromise = import("beautiful-mermaid").then((mod) => {
      var _a;
      const resolved = typeof mod.renderMermaid === "function" ? mod : (_a = mod.default) != null ? _a : mod;
      return resolved;
    });
  }
  cachedModule = await modulePromise;
  return cachedModule;
};
const themeColorCache = /* @__PURE__ */ new Map();
const shikiThemeCache = /* @__PURE__ */ new Map();
const resolveShikiTheme = async (themeName) => {
  const cached = shikiThemeCache.get(themeName);
  if (cached) {
    return cached;
  }
  const highlighter = await shiki.getSingletonHighlighter({
    themes: [themeName]
  });
  const theme = highlighter.getTheme(themeName);
  shikiThemeCache.set(themeName, theme);
  return theme;
};
const renderFallbackCodeBlock = (content) => {
  return `<pre><code class="language-mermaid">${utils.escapeHtml(content)}</code></pre>`;
};
const resolveAsciiOptions = (options) => {
  var _a;
  return {
    ...options,
    useAscii: (_a = options == null ? void 0 : options.useAscii) != null ? _a : false
  };
};
const isThemeConfig = (theme) => {
  return typeof theme === "object" && theme !== null && ("light" in theme || "dark" in theme);
};
const normalizeThemeConfig = (theme) => {
  var _a, _b;
  if (!theme) {
    return void 0;
  }
  if (!isThemeConfig(theme)) {
    return { light: theme, dark: theme };
  }
  const resolvedLight = (_a = theme.light) != null ? _a : theme.dark;
  const resolvedDark = (_b = theme.dark) != null ? _b : theme.light;
  if (resolvedLight === void 0 || resolvedDark === void 0) {
    return void 0;
  }
  return {
    light: resolvedLight,
    dark: resolvedDark
  };
};
const normalizeThemeRegistration = (theme) => {
  if (!theme || typeof theme !== "object") {
    return theme;
  }
  if ("tokenColors" in theme && theme.tokenColors) {
    return theme;
  }
  if ("settings" in theme) {
    const settings = theme.settings;
    if (settings) {
      return {
        ...theme,
        tokenColors: settings
      };
    }
  }
  return theme;
};
const resolveThemeColors = async (theme, module2, context) => {
  if (typeof theme === "string") {
    const cached = themeColorCache.get(theme);
    if (cached) {
      return cached;
    }
    if (module2.THEMES && theme in module2.THEMES) {
      const colors = module2.THEMES[theme];
      if (colors) {
        themeColorCache.set(theme, colors);
        return colors;
      }
    }
    try {
      const shikiTheme = await resolveShikiTheme(theme);
      const colors = module2.fromShikiTheme(shikiTheme);
      themeColorCache.set(theme, colors);
      return colors;
    } catch (error) {
      context.logger.warn(
        "Beautiful Mermaid plugin failed to load Shiki theme:",
        theme,
        error
      );
      return void 0;
    }
  }
  try {
    return module2.fromShikiTheme(normalizeThemeRegistration(theme));
  } catch (error) {
    context.logger.warn(
      "Beautiful Mermaid plugin failed to parse theme object:",
      error
    );
    return void 0;
  }
};
const resolveThemeColorsPair = async (theme, module2, context) => {
  const normalized = normalizeThemeConfig(theme);
  if (!normalized) {
    return void 0;
  }
  const light = normalized.light ? await resolveThemeColors(normalized.light, module2, context) : void 0;
  const dark = normalized.dark ? await resolveThemeColors(normalized.dark, module2, context) : void 0;
  const resolvedLight = light != null ? light : dark;
  const resolvedDark = dark != null ? dark : light;
  if (!resolvedLight || !resolvedDark) {
    return void 0;
  }
  return {
    light: resolvedLight,
    dark: resolvedDark
  };
};
const normalizeCssVarPrefix = (prefix) => {
  if (prefix.startsWith("--")) {
    return prefix;
  }
  return `--${prefix}`;
};
const buildCssVarNames = (prefix) => {
  return {
    bg: `${prefix}-bg`,
    fg: `${prefix}-fg`,
    line: `${prefix}-line`,
    accent: `${prefix}-accent`,
    muted: `${prefix}-muted`,
    surface: `${prefix}-surface`,
    border: `${prefix}-border`
  };
};
const buildCssVarStyle = (colors, vars) => {
  if (!colors) {
    return "";
  }
  const entries = [];
  if (colors.bg) entries.push(`${vars.bg}: ${colors.bg}`);
  if (colors.fg) entries.push(`${vars.fg}: ${colors.fg}`);
  if (colors.line) entries.push(`${vars.line}: ${colors.line}`);
  if (colors.accent) entries.push(`${vars.accent}: ${colors.accent}`);
  if (colors.muted) entries.push(`${vars.muted}: ${colors.muted}`);
  if (colors.surface) entries.push(`${vars.surface}: ${colors.surface}`);
  if (colors.border) entries.push(`${vars.border}: ${colors.border}`);
  return entries.join("; ");
};
const buildCssVarRenderOptions = (vars) => {
  return {
    bg: `var(${vars.bg})`,
    fg: `var(${vars.fg})`,
    line: `var(${vars.line})`,
    accent: `var(${vars.accent})`,
    muted: `var(${vars.muted})`,
    surface: `var(${vars.surface})`,
    border: `var(${vars.border})`
  };
};
const stripColorOptions = (options) => {
  if (!options) {
    return {};
  }
  const { bg, fg, line, accent, muted, surface, border, ...rest } = options;
  return rest;
};
const selectThemeColors = (pair, mode) => {
  var _a, _b, _c;
  if (mode === "dark") {
    return (_a = pair.dark) != null ? _a : pair.light;
  }
  if (mode === "light") {
    return (_b = pair.light) != null ? _b : pair.dark;
  }
  return (_c = pair.light) != null ? _c : pair.dark;
};
const createBeautifulMermaidPlugin = (options = {}) => {
  const {
    output = "svg",
    classPrefix = "beautiful-mermaid",
    includeId = true,
    svgOptions,
    asciiOptions,
    theme,
    themeMode = "auto",
    themeStrategy,
    cssVarPrefix = "--mdc-bm"
  } = options;
  const wrapperClass = `${classPrefix}-wrapper`;
  const svgClass = `${classPrefix}-svg`;
  const asciiClass = `${classPrefix}-ascii`;
  const codeClass = `${classPrefix}-code`;
  const resolvedThemeStrategy = themeStrategy != null ? themeStrategy : themeMode === "auto" ? "css-vars" : "inline";
  const normalizedCssVarPrefix = normalizeCssVarPrefix(cssVarPrefix);
  const cssVarNames = buildCssVarNames(normalizedCssVarPrefix);
  const processBlock = async (content, context) => {
    var _a, _b;
    const trimmedContent = content.trim();
    if (!trimmedContent) {
      context.logger.warn("Beautiful Mermaid plugin received empty content");
      return renderFallbackCodeBlock(content);
    }
    try {
      const module2 = await loadBeautifulMermaid();
      if (output === "ascii") {
        const ascii = module2.renderMermaidAscii(
          trimmedContent,
          resolveAsciiOptions(asciiOptions)
        );
        const escapedAscii = utils.escapeHtml(ascii);
        const idAttribute = includeId ? ` id="${context.getUniqueId()}"` : "";
        return `<pre class="${wrapperClass} ${asciiClass}"${idAttribute}><code class="${codeClass}">${escapedAscii}</code></pre>`;
      }
      let renderOptions = svgOptions;
      let wrapperStyle = "";
      let wrapperStyleBlock = "";
      let scopeId;
      let scopeAttribute = "";
      const themePair = theme ? await resolveThemeColorsPair(theme, module2, context) : void 0;
      if (themePair) {
        if (resolvedThemeStrategy === "inline") {
          if (themeMode === "auto") {
            context.logger.warn(
              'Beautiful Mermaid plugin themeMode "auto" requires themeStrategy "css-vars". Falling back to light mode.'
            );
          }
          const selectedColors = selectThemeColors(
            themePair,
            themeMode === "auto" ? "light" : themeMode
          );
          renderOptions = {
            ...selectedColors,
            ...svgOptions
          };
        } else {
          const baseOptions = stripColorOptions(svgOptions);
          renderOptions = {
            ...baseOptions,
            ...buildCssVarRenderOptions(cssVarNames)
          };
          const lightColors = (_a = themePair.light) != null ? _a : themePair.dark;
          const darkColors = (_b = themePair.dark) != null ? _b : themePair.light;
          const lightStyle = buildCssVarStyle(lightColors, cssVarNames);
          if (themeMode === "auto") {
            const needsScopeId = true;
            if (needsScopeId) {
              scopeId = context.getUniqueId();
              if (includeId) {
                scopeAttribute = ` id="${scopeId}"`;
              } else {
                scopeAttribute = ` data-bm-scope="${scopeId}"`;
              }
            }
            const darkStyle = buildCssVarStyle(darkColors, cssVarNames);
            if (scopeId) {
              const selector = includeId ? `#${scopeId}` : `[data-bm-scope="${scopeId}"]`;
              const lightRule = lightStyle ? `  ${selector} {
    ${lightStyle};
  }
` : "";
              const darkRule = darkStyle ? `  @media (prefers-color-scheme: dark) {
    ${selector} {
      ${darkStyle};
    }
  }
` : "";
              if (lightRule || darkRule) {
                wrapperStyleBlock = `<style>
${lightRule}${darkRule}</style>`;
              }
            }
          } else {
            const selectedColors = selectThemeColors(themePair, themeMode);
            wrapperStyle = buildCssVarStyle(selectedColors, cssVarNames);
          }
        }
      }
      if (!scopeAttribute && includeId) {
        scopeAttribute = ` id="${context.getUniqueId()}"`;
      }
      const styleAttribute = wrapperStyle ? ` style="${wrapperStyle}"` : "";
      const svg = await module2.renderMermaid(trimmedContent, renderOptions);
      return `<div class="${wrapperClass} ${svgClass}"${scopeAttribute}${styleAttribute}>${wrapperStyleBlock}${svg}</div>`;
    } catch (error) {
      context.logger.warn(
        "Beautiful Mermaid plugin failed to render diagram:",
        error
      );
      return renderFallbackCodeBlock(content);
    }
  };
  return {
    name: "mermaid",
    processBlock
  };
};
const createMemoryCacheStorage = () => {
  const cache = /* @__PURE__ */ new Map();
  const mutex = asyncPrimitives.createMutex();
  const cleanupExpiredInternal = (now = Date.now()) => {
    const expiredKeys = [];
    for (const [key, entry] of cache.entries()) {
      if (entry.ttl !== void 0) {
        const isExpired = entry.ttl === 0 || now > entry.timestamp + entry.ttl;
        if (isExpired) {
          expiredKeys.push(key);
        }
      }
    }
    for (const key of expiredKeys) {
      cache.delete(key);
    }
    return expiredKeys;
  };
  const get = async (key) => {
    const entry = cache.get(key);
    if (!entry) {
      return null;
    }
    if (entry.ttl !== void 0) {
      const isExpired = entry.ttl === 0 || Date.now() > entry.timestamp + entry.ttl;
      if (isExpired) {
        const lockHandle = await mutex.lock();
        try {
          const currentEntry = cache.get(key);
          if (currentEntry && currentEntry.ttl !== void 0) {
            const currentTime = Date.now();
            const stillExpired = currentEntry.ttl === 0 || currentTime > currentEntry.timestamp + currentEntry.ttl;
            if (stillExpired) {
              cache.delete(key);
            }
          }
          return null;
        } finally {
          lockHandle.release();
        }
      }
    }
    return entry.data;
  };
  const set = async (key, value, ttl) => {
    const entry = {
      data: value,
      timestamp: Date.now()
    };
    if (ttl !== void 0) {
      entry.ttl = ttl;
    }
    const lockHandle = await mutex.lock();
    try {
      cache.set(key, entry);
    } finally {
      lockHandle.release();
    }
  };
  const deleteEntry = async (key) => {
    const lockHandle = await mutex.lock();
    try {
      cache.delete(key);
    } finally {
      lockHandle.release();
    }
  };
  const clear = async () => {
    const lockHandle = await mutex.lock();
    try {
      cache.clear();
    } finally {
      lockHandle.release();
    }
  };
  const size = async () => {
    const entries = Array.from(cache.entries());
    if (entries.length === 0) {
      return 0;
    }
    const lockHandle = await mutex.lock();
    try {
      cleanupExpiredInternal();
      return cache.size;
    } finally {
      lockHandle.release();
    }
  };
  return {
    get,
    set,
    delete: deleteEntry,
    clear,
    size
  };
};
function generateCacheKey(url, accept, userAgent) {
  const userAgentPart = userAgent || "default";
  return `fetch:${url}:${accept}:${userAgentPart}`;
}
const createCachedFetcher = (userAgent, timeout = 6e4, cacheStorage, options) => {
  const {
    cache = true,
    cacheTTL = 60 * 60 * 1e3,
    // 1 hour default
    cacheFailures = true,
    failureCacheTTL = 5 * 60 * 1e3
    // 5 minutes default for failures
  } = options || {};
  const finalCacheStorage = cache ? cacheStorage || createMemoryCacheStorage() : void 0;
  const fetcherFunction = async (url, accept, signal, logger) => {
    var _a;
    if (cache && finalCacheStorage) {
      const cacheKey = generateCacheKey(url, accept, userAgent);
      const cachedEntryData = await finalCacheStorage.get(cacheKey);
      if (cachedEntryData) {
        try {
          const cacheEntry = JSON.parse(cachedEntryData);
          if (cacheEntry.type === "success") {
            logger == null ? void 0 : logger.info(`Cache HIT (success) for URL: ${url}`);
            return new Response(cacheEntry.data, {
              status: 200,
              statusText: "OK",
              headers: {
                "Content-Type": accept,
                "X-Cache": "HIT"
              }
            });
          } else if (cacheEntry.type === "error" && cacheFailures) {
            logger == null ? void 0 : logger.info(`Cache HIT (error) for URL: ${url}`);
            const error = new Error("Cached error");
            throw error;
          } else {
            logger == null ? void 0 : logger.debug(
              `Cache entry type ${cacheEntry.type} not eligible for use, cacheFailures: ${cacheFailures}`
            );
          }
        } catch (parseError) {
          logger == null ? void 0 : logger.warn(
            `Failed to parse cached entry for URL ${url}:`,
            parseError
          );
          if (parseError instanceof SyntaxError) {
            logger == null ? void 0 : logger.debug("JSON parse error, continuing to network fetch");
          } else {
            throw parseError;
          }
        }
      } else {
        logger == null ? void 0 : logger.info(`Cache MISS for URL: ${url}`);
      }
    }
    let fetchResponse;
    let fetchError = null;
    try {
      fetchResponse = await utils.fetchData(
        url,
        accept,
        userAgent,
        timeout,
        signal,
        logger
      );
    } catch (error) {
      fetchError = error;
      if (cache && finalCacheStorage && cacheFailures) {
        try {
          const cacheKey = generateCacheKey(url, accept, userAgent);
          const errorEntry = {
            type: "error",
            data: "",
            error: {
              message: fetchError.message,
              ...fetchError.message.includes("HTTP error, status:") && {
                status: parseInt(
                  ((_a = fetchError.message.match(/status: (\d+)/)) == null ? void 0 : _a[1]) || "0"
                )
              }
            },
            timestamp: Date.now()
          };
          await finalCacheStorage.set(
            cacheKey,
            JSON.stringify(errorEntry),
            failureCacheTTL
          );
          logger == null ? void 0 : logger.debug(`Cached error for URL: ${url}`);
        } catch (cacheError) {
          logger == null ? void 0 : logger.warn(`Failed to cache error for URL ${url}:`, cacheError);
          console.warn("Failed to cache error:", cacheError);
        }
      }
      throw fetchError;
    }
    if (cache && finalCacheStorage && fetchResponse.ok) {
      try {
        const responseText = await fetchResponse.clone().text();
        const cacheKey = generateCacheKey(url, accept, userAgent);
        const successEntry = {
          type: "success",
          data: responseText,
          timestamp: Date.now()
        };
        await finalCacheStorage.set(
          cacheKey,
          JSON.stringify(successEntry),
          cacheTTL
        );
        logger == null ? void 0 : logger.debug(`Cached successful response for URL: ${url}`);
      } catch (error) {
        logger == null ? void 0 : logger.warn(`Failed to cache response for URL ${url}:`, error);
        console.warn("Failed to cache response:", error);
      }
    }
    return fetchResponse;
  };
  return {
    rawFetcher: fetcherFunction,
    userAgent
  };
};
const createDirectFetcher = (userAgent, timeout = 6e4) => {
  const fetcherFunction = async (url, accept, signal, logger) => {
    logger == null ? void 0 : logger.debug(`Direct fetch for URL: ${url}`);
    return await utils.fetchData(url, accept, userAgent, timeout, signal, logger);
  };
  return {
    rawFetcher: fetcherFunction,
    userAgent
  };
};
exports.fetchData = utils.fetchData;
exports.fetchJson = utils.fetchJson;
exports.fetchText = utils.fetchText;
exports.generateHeadingId = utils.generateHeadingId;
exports.createBeautifulMermaidPlugin = createBeautifulMermaidPlugin;
exports.createCachedFetcher = createCachedFetcher;
exports.createCardPlugin = createCardPlugin;
exports.createDirectFetcher = createDirectFetcher;
exports.createMarkdownProcessor = createMarkdownProcessor;
exports.createMemoryCacheStorage = createMemoryCacheStorage;
exports.createMermaidPlugin = createMermaidPlugin;
exports.createOEmbedPlugin = createOEmbedPlugin;
exports.defaultHtmlOptions = defaultHtmlOptions;
exports.generateCacheKey = generateCacheKey;
exports.getConsoleLogger = getConsoleLogger;
exports.getNoOpLogger = getNoOpLogger;
//# sourceMappingURL=index.cjs.map
